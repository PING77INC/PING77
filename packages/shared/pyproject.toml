[project]
name = "ping77-shared"
version = "0.0.1"
description = "Shared types and utilities for PING77"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0.0",
    "sqlalchemy>=2.0.0",
]

[tool.ruff]
target-version = "py311"
line-length = 100
